[core]
  pager = less -FRX
[user]
  name = Mingyu Gao
  email = mingyu.gao96@gmail.com
[github]
  user = mingyugao
  ; token =
[alias]
  st = status
  cm = commit
  df = diff
  dt = difftool
  br = branch --sort=committerdate
  co = checkout
  cp = cherry-pick
  m = merge
  p = pull
  nb = checkout -b
  db = br -d
  unstage = reset HEAD --
  uncm = reset HEAD~
  last = log -1 HEAD
  logg = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n''          %C(white)%s%C(reset) %C(bold black)- %an%C(reset)' --all
